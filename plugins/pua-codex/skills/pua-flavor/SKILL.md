---
name: pua-flavor
description: "PUA flavor alias for Codex. Codex PUA alias; invoke with `$pua-flavor`."
license: MIT
---

# pua-flavor

This is a Codex alias for the `$pua-flavor` skill.

Read the flavor reference and help the user choose/set a flavor in ~/.pua/config.json without overwriting unrelated fields.

When this alias changes `~/.pua/config.json`, preserve unknown fields and create `~/.pua/` if missing. Do not claim completion without command/output evidence.
