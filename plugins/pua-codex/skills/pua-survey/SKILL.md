---
name: pua-survey
description: "PUA survey alias for Codex. Codex PUA alias; invoke with `$pua-survey`."
license: MIT
---

# pua-survey

This is a Codex alias for the `$pua-survey` skill.

Guide the user through the PUA survey and save the local response. Ask before any upload.

When this alias changes `~/.pua/config.json`, preserve unknown fields and create `~/.pua/` if missing. Do not claim completion without command/output evidence.
